name: Latest Tag

on: release

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - name: Latest tag
        uses: EndBug/latest-tag@v1.5.1
        with:
          description: This tag has been auto-generated by an action.
